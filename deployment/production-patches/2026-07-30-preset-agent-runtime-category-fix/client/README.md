# Client artifact

The binary Client ZIP is retained outside Git. `artifact.json` records its
source revision, successful GitHub Actions run, independent-build hashes,
complete eight-member ZIP contract, protected overlay, seven persistent Agent
IDs, and the production baseline that must match before deployment.
